# Lefthook configuration for pre-commit hooks
# Runs fast checks using Bun on staged files only

pre-commit:
  parallel: true
  commands:
    type-check:
      run: bun run type-check
    lint-staged:
      run: bunx lint-staged
